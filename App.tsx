import { ChromeExtensionToggle } from './components/ChromeExtensionToggle';

export default function App() {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <ChromeExtensionToggle />
    </div>
  );
}